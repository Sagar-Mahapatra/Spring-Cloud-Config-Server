eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: mysql20apr2021
    url: jdbc:mysql://localhost:3306/stock
    username: root
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
management:
  health:
    circuitbreakers:
      enabled: true
  endpoint:
    health:
      show-details: always
logging:
  file:
    name: C:/Users/SAGAR/Desktop/ELK/stock-common.log
  level:
    com: 
      javatechie: DEBUG
      
      

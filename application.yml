spring:
  boot:
    admin:
      client:
        url: http://localhost:9999  
eureka:
  client:
    service-url:
      defaultZone: http://service-registry:8761/eureka/
management:
  endpoints:
    web:
      exposure:
        include: '*'

      
      
    
    
    

      
      
